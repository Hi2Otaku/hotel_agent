---
phase: 05-guest-frontend
plan: 05
type: execute
wave: 4
depends_on: ["05-04"]
files_modified:
  - frontend/src/pages/MyBookings.tsx
  - frontend/src/pages/BookingDetail.tsx
  - frontend/src/components/booking/StatusTimeline.tsx
  - frontend/src/components/booking/StatusBadge.tsx
  - frontend/src/components/booking/CancelDialog.tsx
  - frontend/src/components/booking/ModifyDialog.tsx
autonomous: true
requirements:
  - INFR-01
must_haves:
  truths:
    - "Guest can view a list of their bookings as cards with status badges and action buttons"
    - "Guest can view booking details with status timeline and price breakdown"
    - "Guest can cancel a booking with confirmation dialog"
    - "Guest can modify a booking's dates"
    - "Empty state shows when guest has no bookings"
    - "All booking management pages are responsive"
  artifacts:
    - path: "frontend/src/pages/MyBookings.tsx"
      provides: "Booking list page"
      contains: "No bookings yet"
    - path: "frontend/src/pages/BookingDetail.tsx"
      provides: "Booking detail with timeline"
      contains: "StatusTimeline"
    - path: "frontend/src/components/booking/StatusTimeline.tsx"
      provides: "Visual status progression"
      contains: "Checked Out"
    - path: "frontend/src/components/booking/CancelDialog.tsx"
      provides: "Cancel confirmation dialog"
      contains: "Yes, Cancel Booking"
  key_links:
    - from: "frontend/src/pages/MyBookings.tsx"
      to: "frontend/src/api/booking.ts"
      via: "useBookingList query"
      pattern: "useBookingList"
    - from: "frontend/src/pages/BookingDetail.tsx"
      to: "frontend/src/api/booking.ts"
      via: "useBookingDetails query"
      pattern: "useBookingDetails"
    - from: "frontend/src/components/booking/CancelDialog.tsx"
      to: "frontend/src/api/booking.ts"
      via: "useCancelBooking mutation"
      pattern: "cancelBooking"
---

<objective>
Build booking management pages: My Bookings list with status badges and actions, Booking Detail with status timeline and price breakdown, Cancel and Modify dialogs. Complete the guest journey from search to post-booking management.

Purpose: Booking management completes the guest lifecycle and demonstrates CRUD operations, state machines, and polished UI components.
Output: My Bookings list, Booking Detail page with status timeline, Cancel/Modify dialogs, empty states.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-guest-frontend/05-CONTEXT.md
@.planning/phases/05-guest-frontend/05-UI-SPEC.md
@.planning/phases/05-guest-frontend/05-01-SUMMARY.md
@.planning/phases/05-guest-frontend/05-04-SUMMARY.md

<interfaces>
From frontend/src/hooks/queries/useBookings.ts:
```typescript
export function useBookingList(params?): UseQueryResult<BookingListResponse>;
export function useBookingDetails(id: string): UseQueryResult<BookingResponse>;
export function useCancelBooking(): UseMutationResult;
export function useModifyBooking(): UseMutationResult;
export function useCancellationPolicy(id: string): UseQueryResult<CancellationPolicyResponse>;
```

From frontend/src/api/types.ts:
```typescript
export interface BookingResponse {
  id: string;
  confirmation_number: string;
  user_id: string;
  room_type_id: string;
  check_in: string;
  check_out: string;
  guest_count: number;
  status: string; // "pending" | "confirmed" | "checked_in" | "checked_out" | "cancelled" | "no_show"
  total_price: string | null;
  price_per_night: string | null;
  currency: string;
  nightly_breakdown: NightlyRate[] | null;
  guest_first_name: string | null;
  guest_last_name: string | null;
  guest_email: string | null;
  guest_phone: string | null;
  special_requests: string | null;
  cancelled_at: string | null;
  cancellation_fee: string | null;
  created_at: string;
  updated_at: string;
  // BFF enrichment
  room_type_name?: string;
  room_type_description?: string;
  room_type_photos?: string[];
  room_type_amenities?: string[];
}

export interface CancellationPolicyResponse {
  free_cancellation_before: string | null;
  cancellation_fee: string | null;
  policy_text: string;
}

export interface BookingModifyRequest {
  check_in?: string;
  check_out?: string;
  room_type_id?: string;
  guest_count?: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatusBadge, StatusTimeline, CancelDialog, ModifyDialog components</name>
  <files>
    frontend/src/components/booking/StatusBadge.tsx
    frontend/src/components/booking/StatusTimeline.tsx
    frontend/src/components/booking/CancelDialog.tsx
    frontend/src/components/booking/ModifyDialog.tsx
  </files>
  <read_first>
    frontend/src/api/types.ts
    frontend/src/hooks/queries/useBookings.ts
    frontend/src/lib/formatters.ts
    frontend/src/components/ui/badge.tsx
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/calendar.tsx
    frontend/src/components/ui/popover.tsx
    .planning/phases/05-guest-frontend/05-UI-SPEC.md
  </read_first>
  <action>
1. Create src/components/booking/StatusBadge.tsx:
   - Props: `status: string`
   - Maps status to badge variant and colors:
     - "confirmed": `bg-[#0F766E]/10 text-[#0F766E] border-[#0F766E]/20` (accent/teal)
     - "pending": `bg-amber-100 text-amber-700 border-amber-200`
     - "cancelled": `bg-red-100 text-red-700 border-red-200`
     - "checked_in": `bg-green-100 text-green-700 border-green-200`
     - "checked_out": `bg-slate-100 text-slate-500 border-slate-200` (muted)
     - "no_show": `bg-red-100 text-red-700 border-red-200`
   - Capitalize and format status text: "confirmed" -> "Confirmed", "checked_in" -> "Checked In"
   - Uses shadcn Badge with custom className

2. Create src/components/booking/StatusTimeline.tsx:
   - Props: `status: string`
   - Horizontal timeline with 4 dots connected by lines: Booked > Confirmed > Checked In > Checked Out
   - Step mapping: "pending"=step 0, "confirmed"=step 1, "checked_in"=step 2, "checked_out"=step 3
   - Active dot (current step): filled circle bg `bg-[#0F766E]` (accent), 12px diameter, label below in semibold
   - Completed dot (before current): filled circle bg `bg-[#0F766E]` with lucide Check icon (8px white)
   - Future dot (after current): border only `border-2 border-[#E2E8F0]`, 12px diameter, label in muted #64748B
   - Connecting lines: `h-0.5` between dots, completed=accent, future=slate-200
   - If "cancelled": show all dots as muted, add "Cancelled" label with red icon
   - Label text below each dot: 14px
   - Mobile: reduce spacing, abbreviate labels ("Booked", "Conf.", "In", "Out")
   - Use aria-current="step" on active step

3. Create src/components/booking/CancelDialog.tsx:
   - Props: `bookingId: string`, `open: boolean`, `onOpenChange: (open: boolean) => void`, `onSuccess: () => void`
   - Uses shadcn Dialog
   - Fetches cancellation policy via useCancellationPolicy(bookingId)
   - Content:
     - Heading: "Cancel Booking"
     - Body: "Are you sure you want to cancel this booking? This action cannot be undone."
     - Policy info: if free_cancellation_before is in future, "Free cancellation available." in green. Otherwise "A cancellation fee of {formatCurrency(cancellation_fee)} will apply." in amber.
   - Buttons:
     - "Keep Booking" (outline variant)
     - "Yes, Cancel Booking" (destructive: `bg-[#DC2626]` text-white, hover darken)
   - On "Yes, Cancel": call useCancelBooking mutation, on success: toast "Booking cancelled successfully", call onSuccess callback
   - Loading state: "Yes, Cancel Booking" shows spinner, disabled

4. Create src/components/booking/ModifyDialog.tsx:
   - Props: `booking: BookingResponse`, `open: boolean`, `onOpenChange: (open: boolean) => void`, `onSuccess: () => void`
   - Uses shadcn Dialog (wider, max-w-lg)
   - Form fields:
     - New Check-in date: shadcn Calendar in Popover (default: booking.check_in)
     - New Check-out date: same (default: booking.check_out)
     - New Guest count: Select 1-6 (default: booking.guest_count)
   - "Cancel" (outline) and "Save Changes" (accent) buttons
   - On submit: call useModifyBooking with changed fields only (compare to original)
   - On success: toast showing price difference "Booking modified. Price changed from {old_total} to {new_total} ({price_difference}).", call onSuccess
   - On error: if 409 "This room is no longer available for the new dates." in dialog. Otherwise toast with error.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/booking/StatusBadge.tsx contains "confirmed" and "pending" and "cancelled" and "checked_in" and "checked_out" and "Badge" and "0F766E" or "accent"
    - frontend/src/components/booking/StatusTimeline.tsx contains "Booked" and "Confirmed" and "Checked In" and "Checked Out" and "0F766E" or "accent" and "aria-current" and "Check"
    - frontend/src/components/booking/CancelDialog.tsx contains "Yes, Cancel Booking" and "Keep Booking" and "Are you sure" and "cannot be undone" and "useCancelBooking" and "cancellation_fee" and "Dialog" and "DC2626" or "destructive"
    - frontend/src/components/booking/ModifyDialog.tsx contains "Save Changes" and "Calendar" and "Popover" and "useModifyBooking" and "check_in" and "check_out" and "guest_count" and "price_difference"
  </acceptance_criteria>
  <done>StatusBadge renders color-coded booking status. StatusTimeline shows visual 4-step progression. CancelDialog shows policy and confirms cancellation. ModifyDialog allows date/guest changes with price preview.</done>
</task>

<task type="auto">
  <name>Task 2: Build MyBookings list page and BookingDetail page</name>
  <files>
    frontend/src/pages/MyBookings.tsx
    frontend/src/pages/BookingDetail.tsx
  </files>
  <read_first>
    frontend/src/components/booking/StatusBadge.tsx
    frontend/src/components/booking/StatusTimeline.tsx
    frontend/src/components/booking/CancelDialog.tsx
    frontend/src/components/booking/ModifyDialog.tsx
    frontend/src/hooks/queries/useBookings.ts
    frontend/src/api/types.ts
    frontend/src/lib/formatters.ts
    frontend/src/components/common/EmptyState.tsx
    frontend/src/components/common/PhotoCarousel.tsx
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/skeleton.tsx
    frontend/src/components/ui/separator.tsx
    .planning/phases/05-guest-frontend/05-UI-SPEC.md
  </read_first>
  <action>
1. Create src/pages/MyBookings.tsx:
   - Page heading: "My Bookings" in 24px semibold, padding 32px horizontal (xl)
   - Use useBookingList() hook
   - Card list layout, single column, gap 16px (md)
   - Each booking card:
     - Left section: room type photo thumbnail (80x80, rounded, object-cover) + dates (formatDateRange) + room type name (if available from BFF enrichment)
     - Right section: StatusBadge, confirmation number in muted text (14px), formatCurrency(total_price)
     - Action buttons row at bottom of card:
       - "View Details" button (outline variant) -> navigates to /my-bookings/{booking.id}
       - "Cancel" button (destructive outline variant, `text-[#DC2626] border-[#DC2626]/30`): only visible if status is "pending" or "confirmed". Opens CancelDialog
       - "Modify" button (outline variant): only visible if status is "pending" or "confirmed". Opens ModifyDialog
     - All buttons min-height 44px
   - Loading state: 2 skeleton cards (photo rectangle + 3 text lines + 2 button shapes) using `animate-pulse bg-slate-200`
   - Empty state: EmptyState with heading "No bookings yet", body "Ready to plan your stay? Find the perfect room.", actionLabel "Search Rooms", actionHref "/"
   - Error state: toast with "Something went wrong. Please try again in a moment."
   - On cancel/modify success: invalidate bookings query via queryClient.invalidateQueries

2. Create src/pages/BookingDetail.tsx:
   - Read bookingId from URL params: `useParams().bookingId`
   - Use useBookingDetails(bookingId)
   - Top section:
     - Confirmation number in display size (36px semibold) + StatusBadge to the right
     - StatusTimeline below (full width)
   - Main content: two-column on desktop (lg:grid-cols-3: details 2 cols + photo 1 col)
   - Left content (details):
     - Guest Info section heading (18px semibold):
       - Name: guest_first_name + guest_last_name
       - Email: guest_email
       - Phone: guest_phone
       - Special Requests: special_requests (if any)
     - Separator
     - Stay Details section heading:
       - Dates: formatDateRange(check_in, check_out)
       - Nights: calculateNights(check_in, check_out)
       - Guests: guest_count
       - Room Type: room_type_name (from BFF)
     - Separator
     - Price Breakdown section heading:
       - If nightly_breakdown exists: table showing each night's date + rate using formatCurrency
       - Total: formatCurrency(total_price) in 20px semibold accent color
       - If cancellation_fee: "Cancellation fee: {formatCurrency(cancellation_fee)}" in red
   - Right content: room photo (from room_type_photos BFF data, first photo or placeholder, rounded-lg)
   - Cancellation policy callout (below main content):
     - Card with `bg-amber-50 border-amber-200`, lucide AlertTriangle icon in amber
     - Text: "Free cancellation up to 24 hours before check-in. Late cancellations are subject to a one-night charge."
     - Only show if status is "pending" or "confirmed"
   - Action buttons at bottom:
     - "Cancel Booking" (destructive: `bg-[#DC2626]` text-white): only if status "pending" or "confirmed". Opens CancelDialog
     - "Modify Booking" (outline): only if status "pending" or "confirmed". Opens ModifyDialog
     - "Back to My Bookings" link (muted text) -> navigates to /my-bookings
   - Loading state: skeleton layout (large rect for photo, text lines for details, timeline skeleton)
   - On cancel success: navigate to /my-bookings
   - On modify success: refetch booking details via queryClient.invalidateQueries
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/MyBookings.tsx contains "My Bookings" and "useBookingList" and "StatusBadge" and "View Details" and "Cancel" and "Modify" and "No bookings yet" and "Search Rooms" and "EmptyState" and "Skeleton" and "animate-pulse" and "/my-bookings/"
    - frontend/src/pages/BookingDetail.tsx contains "useBookingDetails" and "StatusTimeline" and "StatusBadge" and "confirmation_number" and "formatCurrency" and "nightly_breakdown" and "Cancel Booking" and "Modify Booking" and "CancelDialog" and "ModifyDialog" and "Free cancellation" and "AlertTriangle" and "lg:grid-cols"
  </acceptance_criteria>
  <done>MyBookings shows card list with status badges, action buttons, and empty state. BookingDetail shows full details with status timeline, price breakdown, cancellation policy, and cancel/modify actions. All states (loading, empty, error) handled.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` passes
- My Bookings shows booking cards with correct status badges
- Empty state shows when no bookings exist
- Booking Detail shows confirmation number, status timeline, price breakdown
- Cancel dialog shows policy and confirms cancellation
- Modify dialog allows date changes
- All pages responsive at mobile/tablet/desktop
</verification>

<success_criteria>
- MyBookings: card list with photo thumbnails, status badges (color-coded), action buttons (View/Cancel/Modify), loading skeletons, empty state with "Search Rooms" CTA
- BookingDetail: confirmation number (36px display), StatusTimeline (4-dot horizontal), guest info, stay details, nightly rate breakdown, room photo, cancellation policy callout, Cancel/Modify buttons
- CancelDialog: confirmation prompt with policy info, destructive red button
- ModifyDialog: date pickers + guest count, price difference feedback
- Status badges use correct colors per UI-SPEC (teal=confirmed, amber=pending, red=cancelled, green=checked_in, muted=checked_out)
</success_criteria>

<output>
After completion, create `.planning/phases/05-guest-frontend/05-05-SUMMARY.md`
</output>
