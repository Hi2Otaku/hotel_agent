---
phase: 05-guest-frontend
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/vitest.config.ts
  - frontend/src/test/setup.ts
  - frontend/src/__tests__/responsive.test.tsx
  - frontend/src/components/layout/__tests__/Navbar.test.tsx
autonomous: true
requirements:
  - INFR-01
must_haves:
  truths:
    - "Vitest test runner executes and reports results for frontend tests"
    - "Responsive layout test validates rendering at mobile, tablet, and desktop widths"
    - "Navbar test validates hamburger menu on mobile and full navigation on desktop"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Vitest configuration with jsdom environment"
      contains: "vitest"
    - path: "frontend/src/test/setup.ts"
      provides: "Testing library setup with cleanup"
      contains: "@testing-library/jest-dom"
    - path: "frontend/src/__tests__/responsive.test.tsx"
      provides: "Responsive layout integration test"
      contains: "mobile"
    - path: "frontend/src/components/layout/__tests__/Navbar.test.tsx"
      provides: "Navbar responsive behavior test"
      contains: "hamburger"
  key_links:
    - from: "frontend/vitest.config.ts"
      to: "frontend/src/test/setup.ts"
      via: "setupFiles config"
      pattern: "setupFiles.*setup"
---

<objective>
Install Vitest dev dependencies and create the test infrastructure needed by all subsequent plans. This includes the Vitest config, testing library setup file, and two behavioral test files for INFR-01 (responsive design).

Purpose: Establish the test harness before any production code is written so that Plans 01-05 can run `npx vitest run` as part of their verify steps.
Output: Working Vitest configuration and two test files (initially failing since components don't exist yet).
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-guest-frontend/05-CONTEXT.md
@.planning/phases/05-guest-frontend/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest dev dependencies and create test infrastructure</name>
  <files>
    frontend/vitest.config.ts
    frontend/src/test/setup.ts
  </files>
  <action>
1. Ensure Vitest and testing library packages are installed as dev dependencies in the frontend project. Plan 05-01 Task 1 already includes these in its install step, but this plan runs first (Wave 0). If the frontend project does not exist yet, create a minimal package.json and install:
   ```bash
   mkdir -p frontend/src/test
   cd frontend
   npm init -y
   npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
   npm install react react-dom
   npm install -D @types/react @types/react-dom
   ```
   NOTE: If the frontend project already exists (from a prior run), just ensure the dev deps are installed.

2. Create `frontend/vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config';
   import path from 'path';

   export default defineConfig({
     test: {
       environment: 'jsdom',
       globals: true,
       setupFiles: ['./src/test/setup.ts'],
       css: true,
       include: ['src/**/*.test.{ts,tsx}'],
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, './src'),
       },
     },
   });
   ```

3. Create `frontend/src/test/setup.ts`:
   ```typescript
   import '@testing-library/jest-dom';
   import { cleanup } from '@testing-library/react';
   import { afterEach } from 'vitest';

   afterEach(() => {
     cleanup();
   });
   ```

4. Add a `"test"` script to frontend/package.json if not present: `"test": "vitest run --reporter=verbose"`
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Vitest runs without configuration errors. Setup file loads @testing-library/jest-dom matchers. Path alias @ resolves correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Create responsive layout and Navbar behavioral test stubs</name>
  <files>
    frontend/src/__tests__/responsive.test.tsx
    frontend/src/components/layout/__tests__/Navbar.test.tsx
  </files>
  <action>
1. Create `frontend/src/__tests__/responsive.test.tsx`:
   This test validates INFR-01 (responsive design). It will initially fail because the components don't exist yet (RED phase). Plan 05-01 Task 2 creates the components that make these pass.

   Test cases (write as `it.skip` or `it.todo` until components exist, then Plan 05-01 will unskip):
   ```typescript
   import { describe, it, expect } from 'vitest';

   describe('Responsive Layout - INFR-01', () => {
     it.todo('renders Navbar and Footer on all pages');

     it.todo('uses mobile layout (stacked, single column) below 640px');

     it.todo('uses tablet layout (adjusted grid) between 640px and 1023px');

     it.todo('uses desktop layout (multi-column grid) at 1024px and above');

     it.todo('search form is full-width on mobile, contained on desktop');

     it.todo('filter drawer renders as Sheet on mobile, sidebar on desktop');
   });
   ```

   NOTE: These are `.todo` stubs. The executor for Plan 05-01 Task 2 should convert relevant todos to real tests using `@testing-library/react` `render()` and window resize simulation. The key point is the test FILE exists and Vitest discovers it.

2. Create `frontend/src/components/layout/__tests__/Navbar.test.tsx`:
   Test cases for Navbar responsive behavior:
   ```typescript
   import { describe, it, expect } from 'vitest';

   describe('Navbar - INFR-01', () => {
     it.todo('displays "HotelBook" logo text');

     it.todo('shows full navigation links on desktop (>=1024px)');

     it.todo('shows hamburger menu icon on mobile (<1024px)');

     it.todo('opens Sheet with navigation links when hamburger is clicked');

     it.todo('shows "My Bookings" link only when authenticated');

     it.todo('shows login button when not authenticated');

     it.todo('all interactive elements have min-height 44px for touch targets');
   });
   ```

   Same approach: `.todo` stubs that Vitest discovers and reports. Plan 05-01 Task 2 should implement the actual test bodies after creating the Navbar component.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Vitest discovers both test files and reports todo test cases. No errors. Test infrastructure is ready for Plans 01-05 to add real test implementations.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run --reporter=verbose` runs without errors
- Output shows test files discovered: `responsive.test.tsx` and `Navbar.test.tsx`
- Todo tests are listed in output
- No configuration errors from jsdom or path aliases
</verification>

<success_criteria>
- Vitest configured with jsdom environment and @testing-library/jest-dom
- Setup file handles cleanup after each test
- Two test files exist with todo stubs for INFR-01 behavioral tests
- `npx vitest run` executes cleanly (exit 0 with todos)
- Path alias `@` configured for imports
</success_criteria>

<output>
After completion, create `.planning/phases/05-guest-frontend/05-00-SUMMARY.md`
</output>
