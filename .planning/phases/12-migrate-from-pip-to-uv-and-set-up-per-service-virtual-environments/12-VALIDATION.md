---
phase: 12
slug: migrate-from-pip-to-uv
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-23
---

# Phase 12 -- Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.3.x + pytest-asyncio 0.23.x |
| **Config file** | Per-service `pyproject.toml` (created in Plan 01 Task 1) |
| **Quick run command** | `cd services/gateway && uv run pytest tests/ -x --tb=short` |
| **Full suite command** | `make test` (runs all 5 services sequentially) |
| **Estimated runtime** | ~60 seconds (gateway only: ~5s, full suite with DB: ~60s) |

---

## Sampling Rate

- **After every task commit:** Run `cd services/gateway && uv run pytest tests/ -x --tb=short` (lightest, no DB)
- **After every plan wave:** Run `make test` (all services) + `docker compose build auth` (Docker smoke)
- **Before `/gsd:verify-work`:** Full suite must be green, all Docker builds succeed
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 12-01-01 | 01 | 1 | UV-01, UV-02 | smoke | `uv lock --check --directory services/auth && uv lock --check --directory services/gateway && cd services/gateway && uv run python -c "import hotelbook_shared"` | Wave 0 (creates pyproject.toml) | pending |
| 12-01-02 | 01 | 1 | UV-03 | integration | `cd services/gateway && uv run pytest tests/ -x --tb=short` | Wave 0 (relocates tests) | pending |
| 12-01-03 | 01 | 1 | UV-03, UV-04 | structural | `test -f services/auth/tests/conftest.py && test -f services/room/tests/conftest.py && test -f services/booking/tests/conftest.py && grep -q "from app" services/auth/tests/conftest.py` | Wave 0 (relocates tests) | pending |
| 12-02-01 | 02 | 2 | UV-05 | smoke | `grep -q "ghcr.io/astral-sh/uv" services/auth/Dockerfile && ! grep -q "pip install" services/auth/Dockerfile` | Existing (Dockerfiles exist) | pending |
| 12-02-02 | 02 | 2 | UV-06, UV-07 | smoke | `grep -q "astral-sh/setup-uv" .github/workflows/ci.yml && grep -q "sync-all" Makefile` | Existing (ci.yml exists) | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] `services/auth/pyproject.toml` -- uv project config with pytest section (Task 12-01-01 creates)
- [ ] `services/room/pyproject.toml` -- uv project config with pytest section (Task 12-01-01 creates)
- [ ] `services/booking/pyproject.toml` -- uv project config with pytest section (Task 12-01-01 creates)
- [ ] `services/gateway/pyproject.toml` -- uv project config with pytest section (Task 12-01-01 creates)
- [ ] `services/chat/pyproject.toml` -- uv project config with pytest section (Task 12-01-01 creates)
- [ ] `services/*/uv.lock` -- lockfiles generated by `uv lock` (Task 12-01-01 creates)
- [ ] `Makefile` -- developer workflow automation (Task 12-02-02 creates)

*Existing infrastructure covers test framework (pytest already installed). Wave 0 creates the per-service project configs that enable `uv run pytest`.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Docker build succeeds end-to-end | UV-05 | Requires Docker daemon running | `docker compose build auth` -- verify exits 0, image created |
| CI pipeline green on push | UV-06 | Requires GitHub Actions run | Push branch, verify all jobs green in GHA UI |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
