---
phase: 04-booking-engine
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - services/booking/app/services/payment.py
  - services/booking/app/services/event_publisher.py
  - services/booking/app/services/email.py
  - services/booking/app/services/pricing.py
  - services/booking/app/services/booking.py
  - services/booking/app/api/v1/bookings.py
  - services/booking/app/main.py
  - tests/booking/__init__.py
  - tests/booking/conftest.py
  - tests/booking/test_booking_flow.py
  - tests/booking/test_payment.py
  - tests/booking/test_email.py
  - pyproject.toml
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02
  - BOOK-03
  - BOOK-04
  - BOOK-05

must_haves:
  truths:
    - "Guest can create a PENDING booking by selecting room type and dates (step 1)"
    - "Guest can submit personal details for a PENDING booking (step 2)"
    - "Guest can submit mock payment to confirm booking and receive confirmation number (step 3)"
    - "Mock payment simulates 2-3 second processing and handles test card outcomes"
    - "Booking events are published to RabbitMQ matching Room service consumer contract"
    - "Confirmation email is sent via Mailpit after payment succeeds"
    - "Cancellation policy information is included in booking response"
  artifacts:
    - path: "services/booking/app/services/booking.py"
      provides: "Core booking logic with state machine, pessimistic locking, 3-step flow"
      contains: "transition_booking"
    - path: "services/booking/app/services/payment.py"
      provides: "Mock payment processing with test cards"
      contains: "TEST_CARDS"
    - path: "services/booking/app/services/event_publisher.py"
      provides: "RabbitMQ event publishing matching Room service consumer"
      contains: "publish_booking_event"
    - path: "services/booking/app/services/email.py"
      provides: "Booking confirmation email via Mailpit"
      contains: "send_booking_confirmation_email"
    - path: "services/booking/app/services/pricing.py"
      provides: "Pricing via Room service API call"
      contains: "get_pricing_from_room_service"
    - path: "services/booking/app/api/v1/bookings.py"
      provides: "3-step booking endpoints"
      exports: ["router"]
    - path: "tests/booking/test_booking_flow.py"
      provides: "Integration tests for 3-step booking flow"
      contains: "test_create_booking"
    - path: "tests/booking/test_email.py"
      provides: "Unit tests for booking confirmation email (BOOK-04)"
      contains: "test_send_booking_confirmation_email"
  key_links:
    - from: "services/booking/app/services/event_publisher.py"
      to: "shared/messaging.py"
      via: "import get_rabbitmq_connection"
      pattern: "from shared.messaging import"
    - from: "services/booking/app/services/booking.py"
      to: "services/booking/app/services/event_publisher.py"
      via: "publish_booking_event call on every state transition"
      pattern: "await publish_booking_event"
    - from: "services/booking/app/api/v1/bookings.py"
      to: "services/booking/app/services/booking.py"
      via: "service layer import"
      pattern: "from app.services.booking import"
---

<objective>
Implement the complete three-step booking flow (reserve, guest details, payment), mock payment processing, RabbitMQ event publishing, confirmation email, and pricing integration with Room service.

Purpose: Deliver the core booking creation lifecycle -- the central feature of Phase 4 -- with pessimistic locking to prevent double-booking, a state machine for valid transitions, and cross-service event integration.
Output: Working 3-step booking API with payment simulation, email confirmation, and event publishing. Integration tests covering the full flow.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-booking-engine/04-CONTEXT.md
@.planning/phases/04-booking-engine/04-RESEARCH.md
@.planning/phases/04-booking-engine/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (models and schemas the executor uses) -->

From services/booking/app/models/booking.py:
```python
class BookingStatus(str, PyEnum):
    PENDING = "pending"
    CONFIRMED = "confirmed"
    CHECKED_IN = "checked_in"
    CHECKED_OUT = "checked_out"
    CANCELLED = "cancelled"
    NO_SHOW = "no_show"

VALID_TRANSITIONS = {
    "pending": {"confirmed", "cancelled"},
    "confirmed": {"checked_in", "cancelled"},
    "checked_in": {"checked_out"},
    "checked_out": set(),
    "cancelled": set(),
    "no_show": set(),
}

class Booking(Base): ...  # Full model with all columns
def generate_confirmation_number() -> str: ...  # Returns "HB-XXXXXX"
```

From services/booking/app/models/payment.py:
```python
class PaymentTransaction(Base): ...  # transaction_id, amount, card_last_four, etc.
```

From services/booking/app/schemas/booking.py:
```python
class BookingCreate: room_type_id, check_in, check_out, guest_count
class GuestDetailsSubmit: guest_first_name, guest_last_name, guest_email, ...
class BookingResponse: ...  # Full response with all fields
class CancellationPolicyResponse: free_cancellation_before, cancellation_fee, policy_text
```

From services/booking/app/schemas/payment.py:
```python
class PaymentSubmit: card_number, expiry_month, expiry_year, cvc, cardholder_name, billing_address
class PaymentResponse: transaction_id, status, amount, currency, card_last_four, card_brand, decline_reason
```

From services/booking/app/core/config.py:
```python
settings.ROOM_SERVICE_URL  # "http://room:8000"
settings.RABBITMQ_URL
settings.CANCELLATION_POLICY_DAYS  # default 3
settings.PENDING_EXPIRY_MINUTES  # default 15
settings.MAIL_SERVER, settings.MAIL_PORT, etc.
```

From services/room/app/services/event_consumer.py (event contract):
```python
# Exchange: "booking.events", routing key: "booking.*"
# Payload must include: event_type, booking_id, room_type_id, room_id,
#   check_in, check_out, status, guest_count
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create service layer (payment, event publisher, email, pricing, core booking logic)</name>
  <read_first>
    - services/booking/app/models/booking.py
    - services/booking/app/models/payment.py
    - services/booking/app/schemas/booking.py
    - services/booking/app/schemas/payment.py
    - services/booking/app/core/config.py
    - services/booking/app/core/database.py
    - services/room/app/services/event_consumer.py
    - services/auth/app/services/email.py
    - shared/shared/messaging.py
  </read_first>
  <files>
    services/booking/app/services/payment.py
    services/booking/app/services/event_publisher.py
    services/booking/app/services/email.py
    services/booking/app/services/pricing.py
    services/booking/app/services/booking.py
  </files>
  <action>
    1. Create `services/booking/app/services/payment.py`:
       - `TEST_CARDS` dict mapping card numbers to outcomes:
         - "4242424242424242": {"status": "succeeded", "brand": "visa"}
         - "4000000000000002": {"status": "declined", "brand": "visa", "reason": "card_declined"}
         - "4111111111111111": {"status": "declined", "brand": "visa", "reason": "insufficient_funds"}
       - `async def process_payment(card_number: str, amount: Decimal, currency: str = "USD") -> dict`:
         - `await asyncio.sleep(2.5)` for simulated processing delay
         - Look up card_number in TEST_CARDS; default to succeeded/visa for unknown cards
         - Generate transaction_id: `f"txn_{uuid.uuid4().hex[:16]}"`
         - Return dict with: transaction_id, status, brand, last_four (card_number[-4:]), amount, currency, decline_reason

    2. Create `services/booking/app/services/event_publisher.py`:
       - `BOOKING_EXCHANGE = "booking.events"`
       - `async def publish_booking_event(booking, event_type: str) -> None`:
         - Build payload dict matching Room service consumer contract EXACTLY:
           ```python
           payload = {
               "event_type": event_type,
               "booking_id": str(booking.id),
               "room_type_id": str(booking.room_type_id),
               "room_id": str(booking.room_id) if booking.room_id else None,
               "check_in": booking.check_in.isoformat(),
               "check_out": booking.check_out.isoformat(),
               "status": booking.status if isinstance(booking.status, str) else booking.status.value,
               "guest_count": booking.guest_count,
           }
           ```
         - Import and use `get_rabbitmq_connection`, `get_channel`, `declare_exchange` from `shared.messaging`
         - Use `settings.RABBITMQ_URL` from `app.core.config`
         - Create `Message(body=json.dumps(payload).encode(), delivery_mode=DeliveryMode.PERSISTENT)`
         - Publish with `routing_key=event_type`
         - Close connection in finally block
         - Wrap entire function in try/except to log errors but not crash the booking flow

    3. Create `services/booking/app/services/email.py`:
       - Mirror auth service email.py pattern exactly:
         - `_conf: ConnectionConfig | None = None`
         - `_get_mail_config()` using `ConnectionConfig.model_construct(...)` with all MAIL_* settings from config
         - `TEMPLATE_FOLDER = Path(__file__).parent.parent / "templates" / "email"`
       - `async def send_booking_confirmation_email(email: str, confirmation_number: str, guest_name: str, check_in: date, check_out: date, room_type_name: str, total_price: Decimal, currency: str, cancellation_policy: str) -> None`:
         - Create MessageSchema with subject "Booking Confirmed - HotelBook"
         - template_body with all params
         - Send via FastMail with template_name="booking_confirmation.html"
         - Wrap in try/except to log errors but not crash booking flow

    4. Create `services/booking/app/services/pricing.py`:
       - `async def get_pricing_from_room_service(room_type_id: UUID, check_in: date, check_out: date, guests: int) -> dict`:
         - Use httpx.AsyncClient(timeout=10.0) to call `{settings.ROOM_SERVICE_URL}/api/v1/search/room-types/{room_type_id}?check_in={check_in}&check_out={check_out}&guests={guests}`
         - Parse response JSON and return dict with:
           - total_price: Decimal(str(data["total_price"]))
           - price_per_night: Decimal(str(data["price_per_night"]))
           - currency: data["currency"]
           - nightly_breakdown: data.get("nightly_rates", [])
           - room_type_name: data.get("name", "Room")
         - On error (httpx.HTTPError), raise HTTPException(502, "Unable to fetch pricing")
       - `async def get_room_count_for_type(room_type_id: UUID) -> int`:
         - Call `{settings.ROOM_SERVICE_URL}/api/v1/rooms/types/{room_type_id}` to get room type details
         - Extract room count from response (or use a fallback query)
         - Cache result in module-level dict with 60s TTL

    5. Create `services/booking/app/services/booking.py`:
       - `async def create_booking(db: AsyncSession, user_id: UUID, data: BookingCreate) -> Booking`:
         - Validate check_in < check_out, check_in >= today
         - Pessimistic locking: SELECT count(*) FROM bookings WHERE room_type_id = data.room_type_id AND status IN ('pending', 'confirmed', 'checked_in') AND check_in < data.check_out AND check_out > data.check_in ... WITH FOR UPDATE
         - Call get_room_count_for_type to get total rooms
         - If blocking_count >= total_rooms, raise HTTPException(409, "No rooms available for selected dates")
         - Get pricing from Room service via get_pricing_from_room_service
         - Create Booking object with: confirmation_number=generate_confirmation_number(), user_id, room_type_id, check_in, check_out, guest_count, status=PENDING, total_price, price_per_night, currency, nightly_breakdown, expires_at=now+15min
         - db.add(booking), await db.flush()
         - Publish "booking.created" event (non-blocking, catch errors)
         - await db.commit(), await db.refresh(booking)
         - Return booking
       - `async def submit_guest_details(db: AsyncSession, booking_id: UUID, user_id: UUID, data: GuestDetailsSubmit) -> Booking`:
         - Get booking, verify ownership (booking.user_id == user_id)
         - Check on-demand expiry (if PENDING and expires_at < now, transition to cancelled)
         - Verify status is PENDING
         - Set guest_first_name, guest_last_name, guest_email, guest_phone, guest_address, special_requests, id_document
         - await db.commit(), await db.refresh(booking)
         - Return booking
       - `async def process_booking_payment(db: AsyncSession, booking_id: UUID, user_id: UUID, payment_data: PaymentSubmit) -> tuple[Booking, PaymentTransaction]`:
         - SELECT booking FOR UPDATE (prevent expiry race)
         - Verify ownership
         - Check on-demand expiry
         - Verify status is PENDING and guest details are filled (guest_first_name is not None)
         - Call process_payment(payment_data.card_number, booking.total_price, booking.currency)
         - Create PaymentTransaction record
         - If payment succeeded: transition_booking to "confirmed"
         - If payment declined: return booking and transaction (don't change status)
         - On success: send confirmation email (non-blocking, catch errors)
         - Return (booking, transaction)
       - `async def transition_booking(db: AsyncSession, booking: Booking, new_status: str, reason: str | None = None) -> Booking`:
         - Validate transition using VALID_TRANSITIONS
         - Update booking.status
         - If cancelled: set cancelled_at, cancellation_reason
         - Publish event: f"booking.{new_status}"
         - await db.commit(), await db.refresh(booking)
         - Return booking
       - `async def get_booking(db: AsyncSession, booking_id: UUID, user_id: UUID | None = None) -> Booking`:
         - Fetch booking by ID
         - If user_id provided, verify ownership
         - On-demand expiry check (if PENDING and expired, transition to cancelled)
         - Return booking
       - `def get_cancellation_policy(booking: Booking, cancellation_policy_days: int) -> CancellationPolicyResponse`:
         - Calculate free_cancellation_before = booking.check_in - timedelta(days=cancellation_policy_days)
         - If today < free_cancellation_before: fee = None, text = f"Free cancellation before {free_cancellation_before}"
         - Else: fee = booking.price_per_night (first night), text = f"Late cancellation: {fee} {booking.currency} fee applies"
         - Return CancellationPolicyResponse
  </action>
  <verify>
    <automated>grep -c "TEST_CARDS" services/booking/app/services/payment.py && grep -c "publish_booking_event" services/booking/app/services/event_publisher.py && grep -c "send_booking_confirmation_email" services/booking/app/services/email.py && grep -c "get_pricing_from_room_service" services/booking/app/services/pricing.py && grep -c "create_booking" services/booking/app/services/booking.py && grep -c "transition_booking" services/booking/app/services/booking.py</automated>
  </verify>
  <acceptance_criteria>
    - `services/booking/app/services/payment.py` contains `TEST_CARDS` dict with keys "4242424242424242", "4000000000000002", "4111111111111111" and `process_payment` function with `await asyncio.sleep(2.5)`
    - `services/booking/app/services/event_publisher.py` contains `BOOKING_EXCHANGE = "booking.events"` and `publish_booking_event` function that builds payload with keys: event_type, booking_id, room_type_id, room_id, check_in, check_out, status, guest_count
    - Event publisher uses `from shared.messaging import get_rabbitmq_connection, get_channel, declare_exchange`
    - `services/booking/app/services/email.py` uses `ConnectionConfig.model_construct` pattern and contains `send_booking_confirmation_email`
    - `services/booking/app/services/pricing.py` calls `{settings.ROOM_SERVICE_URL}/api/v1/search/room-types/` via httpx and returns dict with Decimal total_price and price_per_night
    - `services/booking/app/services/booking.py` contains `create_booking` with `.with_for_update()` call for pessimistic locking
    - `create_booking` sets `expires_at` to now + 15 minutes
    - `transition_booking` validates against `VALID_TRANSITIONS` before changing status
    - `process_booking_payment` uses `SELECT ... FOR UPDATE` on booking row to prevent expiry race
    - `get_booking` performs on-demand expiry check for PENDING bookings
    - All monetary values use Decimal, never float
  </acceptance_criteria>
  <done>Service layer implements complete 3-step booking flow with pessimistic locking, state machine, mock payment with test cards, RabbitMQ event publishing matching Room service contract, and confirmation email via Mailpit</done>
</task>

<task type="auto">
  <name>Task 2: Create booking API endpoints, update main.py, add integration and email tests</name>
  <read_first>
    - services/booking/app/services/booking.py
    - services/booking/app/services/payment.py
    - services/booking/app/services/email.py
    - services/booking/app/schemas/booking.py
    - services/booking/app/schemas/payment.py
    - services/booking/app/api/deps.py
    - services/booking/app/main.py
    - services/room/app/api/v1/rooms.py
    - tests/room/conftest.py
    - pyproject.toml
  </read_first>
  <files>
    services/booking/app/api/v1/bookings.py
    services/booking/app/main.py
    tests/booking/__init__.py
    tests/booking/conftest.py
    tests/booking/test_booking_flow.py
    tests/booking/test_payment.py
    tests/booking/test_email.py
    pyproject.toml
  </files>
  <action>
    1. Create `services/booking/app/api/v1/bookings.py` with router (prefix="/api/v1/bookings", tags=["bookings"]):
       - `POST /` (create_booking_endpoint): Requires get_current_user. Takes BookingCreate body. Calls booking service create_booking. Returns BookingResponse with 201 status.
       - `PUT /{booking_id}/guest-details` (submit_guest_details_endpoint): Requires get_current_user. Takes GuestDetailsSubmit body. Calls booking service submit_guest_details. Returns BookingResponse.
       - `POST /{booking_id}/payment` (process_payment_endpoint): Requires get_current_user. Takes PaymentSubmit body. Calls booking service process_booking_payment. Returns dict with booking (BookingResponse) and payment (PaymentResponse). If payment declined, return 402 with payment details.
       - `GET /{booking_id}` (get_booking_endpoint): Requires get_current_user. Calls booking service get_booking with user_id for ownership check. Returns BookingResponse.
       - `GET /{booking_id}/cancellation-policy` (get_cancellation_policy_endpoint): Requires get_current_user. Gets booking, calls get_cancellation_policy. Returns CancellationPolicyResponse. (Covers BOOK-05)
       - All endpoints extract user_id from `current_user["sub"]` (UUID)

    2. Update `services/booking/app/main.py`:
       - Keep health check
       - Import and include bookings router
       - Add lifespan context manager (placeholder for expiry task -- actual task in Plan 03)
       - Add CORSMiddleware (same as gateway)

    3. Update `pyproject.toml` to add `"services/booking"` to pythonpath:
       ```toml
       pythonpath = ["services/auth", "services/room", "services/booking", "shared"]
       ```

    4. Create `tests/booking/__init__.py` (empty)

    5. Create `tests/booking/conftest.py`:
       - sys.path manipulation to isolate booking service (same pattern as gateway tests)
       - Clear sys.modules["app"] and "app.*" before importing booking app
       - Insert services/booking path at sys.path[0]
       - Import booking app after path setup
       - `@pytest.fixture` for `client` using httpx.AsyncClient with booking app
       - `@pytest.fixture` for mock_db: AsyncSession mock that yields
       - `@pytest.fixture` for `guest_token`: creates a valid JWT claims dict {"sub": str(uuid4()), "role": "guest", "email": "guest@test.com"}
       - `@pytest.fixture` for `guest_user`: returns the claims dict directly
       - Override `get_current_user` dependency to return guest_user
       - Override `get_db` dependency to return mock_db or test db session
       - `@pytest.fixture` to mock `publish_booking_event` (prevent real RabbitMQ calls)
       - `@pytest.fixture` to mock `send_booking_confirmation_email` (prevent real email calls)
       - `@pytest.fixture` to mock `get_pricing_from_room_service` returning test pricing:
         ```python
         {"total_price": Decimal("300.00"), "price_per_night": Decimal("100.00"),
          "currency": "USD", "nightly_breakdown": [], "room_type_name": "Deluxe Room"}
         ```
       - `@pytest.fixture` to mock `get_room_count_for_type` returning 5

    6. Create `tests/booking/test_booking_flow.py`:
       - `test_create_booking_returns_pending`: POST /api/v1/bookings with valid data, assert 201, status=="pending", confirmation_number starts with "HB-", expires_at is set
       - `test_create_booking_requires_auth`: POST without token returns 401
       - `test_submit_guest_details`: PUT /{id}/guest-details with valid data, assert 200, guest_first_name is set
       - `test_submit_guest_details_wrong_user`: PUT with different user returns 403
       - `test_payment_success`: POST /{id}/payment with card 4242424242424242, assert 200, booking status=="confirmed", transaction status=="succeeded"
       - `test_payment_decline`: POST /{id}/payment with card 4000000000000002, assert 402, booking status stays "pending"
       - `test_full_three_step_flow`: Create booking, submit details, process payment -- verify full lifecycle
       - `test_confirmation_number_in_response`: Verify confirmation_number matches HB-[A-Z0-9]{6} pattern
       - `test_cancellation_policy_endpoint`: GET /{id}/cancellation-policy returns policy text

    7. Create `tests/booking/test_payment.py`:
       - `test_process_payment_success`: Direct test of process_payment function with card 4242424242424242
       - `test_process_payment_declined`: Direct test with card 4000000000000002, assert status=="declined", reason=="card_declined"
       - `test_process_payment_insufficient_funds`: Direct test with card 4111111111111111, assert reason=="insufficient_funds"
       - `test_process_payment_unknown_card_succeeds`: Unknown card number defaults to success
       - `test_transaction_id_format`: Verify transaction_id starts with "txn_"

    8. Create `tests/booking/test_email.py` (covers BOOK-04):
       - Mock FastMail to prevent real SMTP calls (patch `fastapi_mail.FastMail.send_message` as async no-op)
       - `test_send_booking_confirmation_email_called_on_payment`: Mock `send_booking_confirmation_email` in conftest, run the full 3-step flow through payment endpoint with card 4242424242424242, assert the mock was called exactly once
       - `test_send_booking_confirmation_email_correct_subject`: Assert the email is created with subject "Booking Confirmed - HotelBook"
       - `test_send_booking_confirmation_email_correct_recipient`: Assert the email is sent to the guest_email from guest details (not the user's account email)
       - `test_send_booking_confirmation_email_includes_confirmation_number`: Assert template_body includes the booking's confirmation_number (HB-XXXXXX format)
       - `test_send_booking_confirmation_email_not_called_on_decline`: Run payment with card 4000000000000002 (decline), assert `send_booking_confirmation_email` was NOT called
       - `test_email_failure_does_not_crash_booking`: Patch `send_booking_confirmation_email` to raise Exception, run payment with success card, assert booking still transitions to CONFIRMED (email errors are caught, not propagated)
  </action>
  <verify>
    <automated>grep -c "router" services/booking/app/api/v1/bookings.py && grep -c "include_router" services/booking/app/main.py && grep -c "services/booking" pyproject.toml && python -m pytest tests/booking/test_payment.py tests/booking/test_email.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `services/booking/app/api/v1/bookings.py` has 5 endpoints: POST /, PUT /{booking_id}/guest-details, POST /{booking_id}/payment, GET /{booking_id}, GET /{booking_id}/cancellation-policy
    - All endpoints use `Depends(get_current_user)` for authentication
    - Payment endpoint returns 402 when payment is declined
    - `services/booking/app/main.py` includes bookings router and has CORSMiddleware
    - `pyproject.toml` pythonpath includes `"services/booking"`
    - `tests/booking/conftest.py` mocks: get_current_user, get_db, publish_booking_event, send_booking_confirmation_email, get_pricing_from_room_service, get_room_count_for_type
    - `tests/booking/test_booking_flow.py` contains at least 7 test functions covering the 3-step flow
    - `tests/booking/test_payment.py` contains tests for all 3 test card numbers and default behavior
    - `tests/booking/test_email.py` contains at least 5 test functions covering: email called on success, correct subject, correct recipient, includes confirmation number, not called on decline, email failure does not crash booking
    - Tests pass: `python -m pytest tests/booking/test_payment.py tests/booking/test_email.py -x -q` exits 0
  </acceptance_criteria>
  <done>Three-step booking API is functional with 5 endpoints, main.py updated, and integration tests verify the full flow, payment behavior, and email confirmation (BOOK-04)</done>
</task>

</tasks>

<verification>
- POST /api/v1/bookings creates a PENDING booking with confirmation number, price snapshot, and expires_at
- PUT /api/v1/bookings/{id}/guest-details saves guest info and verifies ownership
- POST /api/v1/bookings/{id}/payment with 4242424242424242 transitions to CONFIRMED
- POST /api/v1/bookings/{id}/payment with 4000000000000002 returns 402 decline
- GET /api/v1/bookings/{id}/cancellation-policy returns policy details
- All state transitions publish RabbitMQ events (mocked in tests)
- Confirmation email tested: called on success, not called on decline, failure does not crash booking
- Test suite passes: `python -m pytest tests/booking/ -x -q`
</verification>

<success_criteria>
- Complete 3-step booking flow operational: reserve -> guest details -> payment -> confirmed
- Mock payment handles all 3 test cards (success, decline, insufficient funds) with 2.5s delay
- Event publisher emits correct payload matching Room service consumer contract
- Confirmation email service wired with Mailpit pattern
- Confirmation email unit tests verify BOOK-04 requirements
- Pricing fetched from Room service via httpx
- State machine validates all transitions
- Pessimistic locking prevents double-booking
- On-demand expiry check on booking access
- Integration tests cover the full flow and edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/04-booking-engine/04-02-SUMMARY.md`
</output>
