---
phase: 08-testing-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/__tests__/authStore.test.ts
  - frontend/src/__tests__/bookingWizardStore.test.ts
  - frontend/src/__tests__/SearchResults.test.tsx
  - frontend/src/__tests__/BookingWizard.test.tsx
  - frontend-staff/src/components/reports/OccupancyHeatmap.test.tsx
  - frontend-staff/src/components/reports/RevenueChart.test.tsx
  - frontend-staff/src/components/reports/BookingTrendsChart.test.tsx
  - frontend-staff/src/components/reports/DateRangePicker.test.tsx
  - frontend-staff/src/pages/ReportsPage.test.tsx
  - frontend-staff/src/__tests__/LoginPage.test.tsx
  - frontend-staff/src/__tests__/ReservationsPage.test.tsx
  - e2e/package.json
  - e2e/playwright.config.ts
  - e2e/tests/guest-booking.spec.ts
  - e2e/tests/staff-checkin.spec.ts
autonomous: true
requirements:
  - INFR-02

must_haves:
  truths:
    - "Frontend component tests pass for guest auth store, booking wizard store, search results, and booking wizard"
    - "Staff frontend report stubs are converted to real passing tests"
    - "Playwright E2E project is initialized with guest booking and staff check-in test specs"
    - "All Vitest suites pass: cd frontend && npm test && cd ../frontend-staff && npm test"
    - "Backend auth tests pass confirming conftest.py fixture is healthy: pytest tests/auth -x"
  artifacts:
    - path: "frontend/src/__tests__/authStore.test.ts"
      provides: "Auth store unit tests"
      contains: "describe.*authStore"
    - path: "frontend/src/__tests__/BookingWizard.test.tsx"
      provides: "Booking wizard component tests"
      contains: "describe.*BookingWizard"
    - path: "frontend-staff/src/components/reports/OccupancyHeatmap.test.tsx"
      provides: "Real Nivo chart tests replacing it.todo stubs"
      contains: "expect"
    - path: "e2e/playwright.config.ts"
      provides: "Playwright configuration"
      contains: "defineConfig"
    - path: "e2e/tests/guest-booking.spec.ts"
      provides: "Guest booking E2E flow"
      contains: "test.describe.*Guest Booking"
  key_links:
    - from: "frontend/src/__tests__/authStore.test.ts"
      to: "frontend/src/stores/authStore.ts"
      via: "import and exercise store methods"
      pattern: "import.*authStore"
    - from: "e2e/playwright.config.ts"
      to: "http://localhost:5173"
      via: "baseURL configuration"
      pattern: "baseURL.*localhost"
---

<objective>
Fill all frontend test gaps and create the Playwright E2E test project.

Purpose: INFR-02 requires comprehensive tests across all services and frontends. The backend already has 111 tests but guest frontend has only 2 tests and staff frontend has 0 real tests (5 stubs). This plan adds ~20 frontend component/store tests and creates 2 E2E specs.

Output: Passing guest and staff frontend test suites, initialized Playwright project with E2E test files.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-testing-deployment/08-CONTEXT.md
@.planning/phases/08-testing-deployment/08-RESEARCH.md
@.planning/phases/08-testing-deployment/08-VALIDATION.md
@frontend/vitest.config.ts
@frontend-staff/vitest.config.ts
@frontend/src/test/setup.ts
@frontend-staff/src/test/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guest frontend component and store tests</name>
  <files>
    frontend/src/__tests__/authStore.test.ts,
    frontend/src/__tests__/bookingWizardStore.test.ts,
    frontend/src/__tests__/SearchResults.test.tsx,
    frontend/src/__tests__/BookingWizard.test.tsx
  </files>
  <read_first>
    frontend/src/stores/authStore.ts,
    frontend/src/stores/bookingWizardStore.ts,
    frontend/src/pages/SearchResults.tsx,
    frontend/src/pages/BookingWizard.tsx,
    frontend/src/test/setup.ts,
    frontend/vitest.config.ts
  </read_first>
  <action>
    **Guest frontend tests (4 new files):**

    1. `frontend/src/__tests__/authStore.test.ts` -- Unit tests for Zustand auth store:
       - Test login sets token and user in state
       - Test logout clears token, user, and localStorage
       - Test isAuthenticated computed from token presence
       - Mock axios and localStorage; test store methods directly via `useAuthStore.getState()`

    2. `frontend/src/__tests__/bookingWizardStore.test.ts` -- Unit tests for booking wizard store:
       - Test setStep advances step number
       - Test setRoomSelection stores room data
       - Test setGuestDetails stores guest info
       - Test reset clears all wizard state
       - Exercise store directly via `getState()`

    3. `frontend/src/__tests__/SearchResults.test.tsx` -- Component test:
       - Mock `frontend/src/api/search.ts` module
       - Render SearchResults inside MemoryRouter
       - Test renders loading state, renders room cards when data resolves
       - Test filter interactions (check that filter state changes trigger re-render)

    4. `frontend/src/__tests__/BookingWizard.test.tsx` -- Component test:
       - Mock `frontend/src/api/booking.ts` module
       - Render BookingWizard inside MemoryRouter with route params
       - Test renders first step initially
       - Test step navigation (next/back buttons visible)

    **Testing conventions:**
    - Use `vi.mock()` for API modules
    - Use `render()` from `@testing-library/react` with MemoryRouter wrapper
    - Use `screen.getByRole`, `screen.getByText`, `screen.getByPlaceholderText` for queries
    - Follow existing test setup pattern (localStorage polyfill already in setup.ts)
    - Each describe block should have 3-5 test cases covering render, interaction, and edge cases

    **Conftest verification:** Before writing frontend tests, run `pytest tests/auth -x --tb=short` to confirm the backend conftest.py async session fixture is healthy. If it fails, diagnose and fix `tests/conftest.py` before proceeding.
  </action>
  <verify>
    <automated>pytest tests/auth -x --tb=short 2>&1 | tail -5 && cd frontend && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/auth -x` passes (confirms conftest.py fixture is healthy)
    - frontend/src/__tests__/authStore.test.ts exists with `describe('authStore'` and at least 3 `it(` or `test(` blocks
    - frontend/src/__tests__/bookingWizardStore.test.ts exists with `describe('bookingWizardStore'` or `describe('Booking`
    - frontend/src/__tests__/SearchResults.test.tsx exists with `describe('SearchResults'`
    - frontend/src/__tests__/BookingWizard.test.tsx exists with `describe('BookingWizard'`
    - `cd frontend && npm test -- --run` exits 0
  </acceptance_criteria>
  <done>Guest frontend has 6+ passing test files (2 existing + 4 new). Backend auth tests pass confirming conftest.py is healthy.</done>
</task>

<task type="auto">
  <name>Task 2: Staff frontend component tests (convert stubs + new pages)</name>
  <files>
    frontend-staff/src/components/reports/OccupancyHeatmap.test.tsx,
    frontend-staff/src/components/reports/RevenueChart.test.tsx,
    frontend-staff/src/components/reports/BookingTrendsChart.test.tsx,
    frontend-staff/src/components/reports/DateRangePicker.test.tsx,
    frontend-staff/src/pages/ReportsPage.test.tsx,
    frontend-staff/src/__tests__/LoginPage.test.tsx,
    frontend-staff/src/__tests__/ReservationsPage.test.tsx
  </files>
  <read_first>
    frontend-staff/src/components/reports/OccupancyHeatmap.tsx,
    frontend-staff/src/components/reports/OccupancyHeatmap.test.tsx,
    frontend-staff/src/components/reports/RevenueChart.tsx,
    frontend-staff/src/components/reports/RevenueChart.test.tsx,
    frontend-staff/src/components/reports/BookingTrendsChart.tsx,
    frontend-staff/src/components/reports/BookingTrendsChart.test.tsx,
    frontend-staff/src/components/reports/DateRangePicker.tsx,
    frontend-staff/src/components/reports/DateRangePicker.test.tsx,
    frontend-staff/src/pages/ReportsPage.tsx,
    frontend-staff/src/pages/ReportsPage.test.tsx,
    frontend-staff/src/pages/LoginPage.tsx,
    frontend-staff/src/pages/ReservationsPage.tsx,
    frontend-staff/src/test/setup.ts,
    frontend-staff/vitest.config.ts
  </read_first>
  <action>
    **Convert 5 stub files to real tests:**

    1. `OccupancyHeatmap.test.tsx`: Replace it.todo with real tests -- render component with mock data, verify ResponsiveCalendar is called (vi.mock returns vi.fn), assert props passed include `from` and `to` dates. Keep the existing Nivo mock pattern `vi.mock('@nivo/calendar', () => ({ ResponsiveCalendar: vi.fn(() => null) }))`.

    2. `RevenueChart.test.tsx`: Render with mock revenue data, verify Nivo bar chart mock called, test date range affects display.

    3. `BookingTrendsChart.test.tsx`: Render with mock trends data, verify Nivo line chart mock called.

    4. `DateRangePicker.test.tsx`: Render, verify date inputs present, test onChange callback fires with new range.

    5. `ReportsPage.test.tsx`: Render full page, verify KPI cards and chart sections render. Mock the API layer.

    **Create 2 new page tests:**

    6. `frontend-staff/src/__tests__/LoginPage.test.tsx` -- Component test:
        - Render LoginPage in MemoryRouter
        - Verify email and password inputs present
        - Test submit button exists and form renders

    7. `frontend-staff/src/__tests__/ReservationsPage.test.tsx` -- Component test:
        - Mock the staff API module
        - Render ReservationsPage in MemoryRouter
        - Verify search input, filter dropdowns, and table structure render

    **Testing conventions:**
    - Use `vi.mock()` for API modules and Nivo chart components
    - Use `render()` from `@testing-library/react` with MemoryRouter wrapper
    - Use `screen.getByRole`, `screen.getByText`, `screen.getByPlaceholderText` for queries
    - Follow existing test setup pattern (localStorage polyfill already in setup.ts)
    - Each describe block should have 3-5 test cases covering render, interaction, and edge cases
  </action>
  <verify>
    <automated>cd frontend-staff && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend-staff/src/components/reports/OccupancyHeatmap.test.tsx contains `expect(` (not just it.todo)
    - frontend-staff/src/components/reports/RevenueChart.test.tsx contains `expect(`
    - frontend-staff/src/components/reports/BookingTrendsChart.test.tsx contains `expect(`
    - frontend-staff/src/components/reports/DateRangePicker.test.tsx contains `expect(`
    - frontend-staff/src/pages/ReportsPage.test.tsx contains `expect(`
    - frontend-staff/src/__tests__/LoginPage.test.tsx exists with `describe('LoginPage'`
    - frontend-staff/src/__tests__/ReservationsPage.test.tsx exists with `describe('Reservations`
    - `cd frontend-staff && npm test -- --run` exits 0
    - No `it.todo` remains in any staff frontend test file
  </acceptance_criteria>
  <done>Staff frontend has 7+ passing test files (5 converted report stubs + 2 new page tests). All Vitest suites pass with no remaining it.todo stubs.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E project setup with test specs</name>
  <files>
    e2e/package.json,
    e2e/playwright.config.ts,
    e2e/tests/guest-booking.spec.ts,
    e2e/tests/staff-checkin.spec.ts
  </files>
  <read_first>
    docker-compose.yml,
    frontend/src/pages/Landing.tsx,
    frontend/src/pages/BookingWizard.tsx,
    frontend-staff/src/pages/LoginPage.tsx,
    frontend-staff/src/pages/ReservationsPage.tsx,
    .planning/phases/08-testing-deployment/08-RESEARCH.md
  </read_first>
  <action>
    **Create the e2e/ project directory:**

    1. `e2e/package.json`:
       ```json
       {
         "name": "hotelbook-e2e",
         "private": true,
         "devDependencies": {
           "@playwright/test": "^1.58.0"
         },
         "scripts": {
           "test": "playwright test",
           "test:headed": "playwright test --headed"
         }
       }
       ```

    2. `e2e/playwright.config.ts`:
       - testDir: './tests'
       - timeout: 60_000
       - retries: 1
       - use.baseURL: 'http://localhost:5173' (guest frontend)
       - use.trace: 'on-first-retry'
       - use.screenshot: 'only-on-failure'
       - projects: [{ name: 'chromium', use: { browserName: 'chromium' } }]
       - webServer: command 'docker compose up -d', url 'http://localhost:8000/health', reuseExistingServer true, timeout 120_000

    3. `e2e/tests/guest-booking.spec.ts` -- Guest booking flow E2E:
       - test.describe('Guest Booking Flow')
       - Test: 'search for available rooms' -- navigate to /, fill check-in date, fill check-out date, fill guest count, click search, expect room cards visible
       - Test: 'view room details' -- from search results, click first room card, expect room detail page with amenities and pricing
       - Test: 'complete booking from search to confirmation' -- full flow: search -> select room -> fill guest details -> fill mock payment (card 4242424242424242, any future expiry, any CVV) -> submit -> expect confirmation number visible (data-testid="confirmation-number" or text matching HB-XXXXXX pattern)
       - Test: 'register and login' -- navigate to /register, fill email/password/name, submit, expect redirect to home or dashboard

    4. `e2e/tests/staff-checkin.spec.ts` -- Staff check-in/out E2E:
       - test.describe('Staff Check-in/out Flow')
       - Use baseURL override: test.use({ baseURL: 'http://localhost:5174' }) for staff frontend
       - Test: 'staff login' -- navigate to /, fill email (admin@hotel.local), password (admin123), submit, expect dashboard/overview visible
       - Test: 'view reservations list' -- after login, navigate to reservations, expect table with reservation rows
       - Test: 'check in a guest' -- from reservations, find a confirmed booking, click check-in, assign room from dropdown, confirm, expect status change to checked_in

    **Install dependencies (run after creating files):**
    ```bash
    cd e2e && npm install
    npx playwright install --with-deps chromium
    ```

    Note: E2E tests require the full Docker Compose stack running. They will be verified in CI, not locally as part of this task. The task verifies the files are valid TypeScript and Playwright recognizes them.
  </action>
  <verify>
    <automated>cd e2e && npm install && npx playwright install --with-deps chromium 2>&1 | tail -5 && npx playwright test --list 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - e2e/package.json exists with `@playwright/test` in devDependencies
    - e2e/playwright.config.ts exists with `defineConfig` and `baseURL.*localhost:5173`
    - e2e/tests/guest-booking.spec.ts exists with `test.describe('Guest Booking`
    - e2e/tests/staff-checkin.spec.ts exists with `test.describe('Staff`
    - `cd e2e && npx playwright test --list` shows at least 5 test entries
  </acceptance_criteria>
  <done>Playwright project initialized in e2e/ with chromium installed, 2 spec files containing 6+ E2E tests, and `npx playwright test --list` enumerates all tests without errors.</done>
</task>

</tasks>

<verification>
- `pytest tests/auth -x --tb=short` exits 0 (conftest.py fixture healthy)
- `cd frontend && npm test -- --run` exits 0 with 6+ test files
- `cd frontend-staff && npm test -- --run` exits 0 with 7+ test files
- `cd e2e && npx playwright test --list` enumerates 5+ tests
- No `it.todo` remains in staff frontend report test files
</verification>

<success_criteria>
- Backend auth tests pass (conftest.py fixture confirmed healthy)
- Guest frontend: 6+ passing Vitest test files (existing Navbar, responsive + new authStore, bookingWizardStore, SearchResults, BookingWizard)
- Staff frontend: 7+ passing Vitest test files (5 converted report stubs + LoginPage + ReservationsPage)
- E2E: Playwright project with 2 spec files, chromium installed, tests listable
- Total frontend tests: 20+ (up from 2 real tests)
</success_criteria>

<output>
After completion, create `.planning/phases/08-testing-deployment/08-01-SUMMARY.md`
</output>
