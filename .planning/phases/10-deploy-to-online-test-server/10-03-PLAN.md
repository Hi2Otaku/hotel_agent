---
phase: 10-deploy-to-online-test-server
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - nginx/conf.d/default.conf
  - .github/workflows/ci.yml
autonomous: false
requirements: [DEPLOY-01, DEPLOY-04]

must_haves:
  truths:
    - "Nginx config has no SSL references active (all commented or removed)"
    - "Deploy job includes a health check after docker compose up"
    - "User can verify the full stack is accessible at EC2_IP:80"
  artifacts:
    - path: "nginx/conf.d/default.conf"
      provides: "HTTP-only nginx config confirmed clean"
      contains: "listen 80"
    - path: ".github/workflows/ci.yml"
      provides: "Deploy job with post-deploy health check"
      contains: "curl"
  key_links:
    - from: "nginx/conf.d/default.conf"
      to: "gateway:8000"
      via: "proxy_pass for /api/"
      pattern: "proxy_pass http://gateway:8000"
    - from: "nginx/conf.d/default.conf"
      to: "frontend-guest:80"
      via: "proxy_pass for / catch-all"
      pattern: "proxy_pass http://frontend-guest:80"
---

<objective>
Finalize nginx for HTTP-only serving, add a post-deploy health check to CI/CD, and verify the deployment end-to-end.

Purpose: Ensure the deployed stack is confirmed working after every CI/CD run, and the nginx config cleanly serves HTTP without any SSL artifacts that could cause confusion.
Output: Clean nginx config, deploy health check, human verification of live server.
</objective>

<execution_context>
@.planning/phases/10-deploy-to-online-test-server/10-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@nginx/conf.d/default.conf
@.github/workflows/ci.yml
@.planning/phases/10-deploy-to-online-test-server/10-01-SUMMARY.md
@.planning/phases/10-deploy-to-online-test-server/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean nginx config and add post-deploy health check to CI/CD</name>
  <files>nginx/conf.d/default.conf, .github/workflows/ci.yml</files>
  <read_first>
    - nginx/conf.d/default.conf
    - .github/workflows/ci.yml
  </read_first>
  <action>
**Nginx config cleanup:**

The current nginx config at `nginx/conf.d/default.conf` already listens on port 80 and has SSL lines commented out. Verify it is clean:
- `listen 80;` is active (not commented)
- `listen 443 ssl;` line remains commented
- SSL certificate lines remain commented
- The HTTP->HTTPS redirect block remains commented
- All proxy_pass directives are intact: /api/ -> gateway:8000, /staff/ -> frontend-staff:80, / -> frontend-guest:80, /docs and /openapi.json -> gateway:8000, /health -> gateway:8000

If the config is already clean (it should be based on current state), leave it unchanged. Only modify if there's an active SSL directive that would cause nginx to fail without certificates.

**Post-deploy health check:**

Add a health check step to the deploy job in `.github/workflows/ci.yml` AFTER the SSH deploy step. Use a second `appleboy/ssh-action` step that waits for services to be healthy:

```yaml
- name: Verify deployment health
  uses: appleboy/ssh-action@v1
  with:
    host: ${{ secrets.EC2_HOST }}
    username: ${{ secrets.EC2_USER }}
    key: ${{ secrets.EC2_SSH_KEY }}
    script: |
      echo "Waiting for services to start..."
      sleep 15

      # Check health endpoint
      HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost/health)
      if [ "$HTTP_STATUS" = "200" ]; then
        echo "Health check passed (HTTP $HTTP_STATUS)"
      else
        echo "Health check FAILED (HTTP $HTTP_STATUS)"
        docker compose -f docker-compose.yml -f docker-compose.prod.yml logs --tail=50
        exit 1
      fi

      # Check guest frontend serves HTML
      GUEST_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost/)
      echo "Guest frontend: HTTP $GUEST_STATUS"

      # Check staff frontend
      STAFF_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost/staff/)
      echo "Staff frontend: HTTP $STAFF_STATUS"

      echo "Deployment verified successfully"
```

This step goes AFTER the existing "Deploy to EC2" step, within the same `deploy` job.
  </action>
  <verify>
    <automated>bash -c "grep -q 'Verify deployment health' .github/workflows/ci.yml && grep -q 'health' .github/workflows/ci.yml && grep -q 'listen 80' nginx/conf.d/default.conf && echo PASS"</automated>
  </verify>
  <acceptance_criteria>
    - nginx/conf.d/default.conf has `listen 80;` as the only active listen directive
    - nginx/conf.d/default.conf does NOT have an uncommented `listen 443` line
    - .github/workflows/ci.yml deploy job contains a "Verify deployment health" step
    - Health check step curls `http://localhost/health` and checks for HTTP 200
    - Health check step prints docker compose logs on failure (`exit 1`)
    - Health check step is a second `appleboy/ssh-action@v1` invocation after the deploy step
  </acceptance_criteria>
  <done>Nginx serves HTTP-only. Every CI/CD deploy verifies the health endpoint returns 200 before marking success.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deployment readiness</name>
  <what-built>
Complete EC2 deployment infrastructure:
- scripts/setup-server.sh — one-time server provisioning (Docker, swap, repo clone)
- .env.production.template — documents all required secrets
- .github/workflows/ci.yml — deploy job writes .env + JWT keys from GitHub Secrets, post-deploy health check
- docker-compose.prod.yml — env-var overrides for all passwords, HTTP-only (no SSL)
- services/auth/app/services/seed_guests.py — 8 demo guest accounts seeded on startup
- nginx/conf.d/default.conf — clean HTTP-only config
  </what-built>
  <how-to-verify>
1. Review `scripts/setup-server.sh` — confirm it installs Docker, creates 2GB swap, clones repo to /opt/hotelbook
2. Review `.github/workflows/ci.yml` deploy job — confirm it writes .env and JWT keys from secrets
3. Review `docker-compose.prod.yml` — confirm DB passwords use `${VAR:-default}` pattern
4. Run locally to verify no regressions: `docker compose up -d --build` (dev compose should still work with defaults)
5. Verify `docker compose -f docker-compose.yml -f docker-compose.prod.yml config` shows correct merged config

**To actually deploy (when ready):**
1. Provision EC2 t3.small in us-east-1 (Ubuntu 22.04 AMI)
2. Configure security group: inbound ports 22 (SSH) and 80 (HTTP)
3. SSH in and run: `bash scripts/setup-server.sh`
4. Configure all 11 GitHub Secrets listed in CONTEXT.md
5. Push to main branch — CI/CD will deploy automatically
6. Visit http://EC2_PUBLIC_IP/ to see guest frontend
7. Visit http://EC2_PUBLIC_IP/staff/ to see staff dashboard
8. Log in with admin credentials to verify staff dashboard has booking data
  </how-to-verify>
  <resume-signal>Type "approved" if deployment infrastructure looks correct, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Nginx config serves HTTP-only with no active SSL directives
- CI/CD deploy job verifies deployment health after every push
- Full stack accessible at EC2_IP:80 (guest frontend, staff dashboard, API)
</verification>

<success_criteria>
- Health check confirms /health returns 200 after deployment
- Guest frontend loads at /
- Staff dashboard loads at /staff/
- Admin can log in and see seeded booking data
</success_criteria>

<output>
After completion, create `.planning/phases/10-deploy-to-online-test-server/10-03-SUMMARY.md`
</output>
