---
phase: 10-deploy-to-online-test-server
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [.env.production.template]
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]
gap_closure: true

must_haves:
  truths:
    - ".env.production.template documents JWT_PRIVATE_KEY and JWT_PUBLIC_KEY with CHANGE_ME placeholders"
    - ".env.production.template does not contain MinIO variables (MinIO is dev-only, not in prod compose)"
    - "Template lists all 8 required production secrets matching the CI/CD envs parameter"
  artifacts:
    - path: ".env.production.template"
      provides: "Production environment variable documentation"
      contains: "JWT_PRIVATE_KEY"
  key_links:
    - from: ".env.production.template"
      to: ".github/workflows/ci.yml"
      via: "Secret names must match CI/CD envs list"
      pattern: "JWT_PRIVATE_KEY|JWT_PUBLIC_KEY"
---

<objective>
Fix .env.production.template to document JWT key secrets and remove dev-only MinIO variables.

Purpose: UAT Test 2 found that the production environment template is missing JWT_PRIVATE_KEY and JWT_PUBLIC_KEY entries but includes MINIO variables that are not used in the production compose. The template must accurately document all secrets required for production deployment.
Output: Corrected .env.production.template matching the 11 GitHub Secrets listed in CONTEXT.md (minus the 3 SSH/host secrets which are CI-only, not .env file secrets).
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-deploy-to-online-test-server/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix production environment template to match actual production secrets</name>
  <files>.env.production.template</files>
  <read_first>.env.production.template, .github/workflows/ci.yml (lines 185-215 for the deploy job envs list)</read_first>
  <action>
Update `.env.production.template` to accurately document all production secrets:

1. KEEP these existing entries (they are correct):
   - AUTH_DB_PASSWORD=CHANGE_ME
   - ROOM_DB_PASSWORD=CHANGE_ME
   - BOOKING_DB_PASSWORD=CHANGE_ME
   - RABBITMQ_PASS=CHANGE_ME
   - ADMIN_EMAIL=admin@hotelbook.com
   - ADMIN_PASSWORD=CHANGE_ME

2. ADD these missing entries with a "# JWT Keys (RS256 PEM format)" section header:
   - JWT_PRIVATE_KEY=CHANGE_ME
   - JWT_PUBLIC_KEY=CHANGE_ME
   Add a comment noting these should contain the full PEM content (the CI/CD deploy job writes them to files on the server).

3. REMOVE the MinIO section entirely (MINIO_ROOT_USER, MINIO_ROOT_PASSWORD). MinIO is a dev-only service -- it appears in docker-compose.yml but NOT in docker-compose.prod.yml. Including it in the production template is misleading.

4. Verify the final list of 8 .env secrets matches the CI/CD deploy job envs parameter in ci.yml (AUTH_DB_PASSWORD, ROOM_DB_PASSWORD, BOOKING_DB_PASSWORD, RABBITMQ_PASS, ADMIN_EMAIL, ADMIN_PASSWORD, JWT_PRIVATE_KEY, JWT_PUBLIC_KEY).
  </action>
  <verify>
    <automated>bash -c "grep -q 'JWT_PRIVATE_KEY' .env.production.template && grep -q 'JWT_PUBLIC_KEY' .env.production.template && ! grep -q 'MINIO' .env.production.template && echo PASS || echo FAIL"</automated>
  </verify>
  <acceptance_criteria>
- JWT_PRIVATE_KEY and JWT_PUBLIC_KEY appear in the template with CHANGE_ME placeholders
- No MINIO variables present in the template
- All 8 production secrets documented (matching CI/CD envs parameter)
- Template has clear section headers for each group of variables
  </acceptance_criteria>
  <done>Template documents all 8 production secrets including JWT keys, no dev-only MinIO variables present</done>
</task>

</tasks>

<verification>
- `grep -c 'CHANGE_ME' .env.production.template` returns 7 (all except ADMIN_EMAIL have CHANGE_ME)
- `grep 'JWT_PRIVATE_KEY\|JWT_PUBLIC_KEY' .env.production.template` shows both JWT entries
- `grep 'MINIO' .env.production.template` returns no matches
- The 8 variable names in the template match the 8 names in the CI/CD deploy job envs parameter
</verification>

<success_criteria>
UAT Test 2 passes: ".env.production.template documents JWT_PRIVATE_KEY and JWT_PUBLIC_KEY" is true, and the template no longer contains misleading MinIO variables.
</success_criteria>

<output>
After completion, create `.planning/phases/10-deploy-to-online-test-server/10-04-SUMMARY.md`
</output>
