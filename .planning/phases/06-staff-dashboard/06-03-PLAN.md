---
phase: 06-staff-dashboard
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - frontend-staff/src/pages/OverviewPage.tsx
  - frontend-staff/src/pages/ReservationsPage.tsx
  - frontend-staff/src/components/dashboard/MetricCard.tsx
  - frontend-staff/src/components/dashboard/ArrivalsList.tsx
  - frontend-staff/src/components/dashboard/DeparturesList.tsx
  - frontend-staff/src/components/reservations/ReservationCard.tsx
  - frontend-staff/src/components/reservations/SearchFilters.tsx
autonomous: true
requirements: [STAF-01]

must_haves:
  truths:
    - "Staff can see today's overview with 4 metric cards (check-ins, check-outs, occupancy, rooms to clean)"
    - "Staff can see today's arrivals list with Check In button on each"
    - "Staff can see today's departures list with Check Out button on each"
    - "Staff can view all reservations in a card grid (3 cols desktop, 2 tablet, 1 mobile)"
    - "Staff can search reservations by guest name or confirmation number"
    - "Staff can filter reservations by status and date range"
    - "Staff can paginate reservation list at 12 cards per page"
    - "Quick action buttons are contextual: Check In for confirmed, Check Out for checked_in"
  artifacts:
    - path: "frontend-staff/src/pages/OverviewPage.tsx"
      provides: "Today's operational dashboard"
      contains: "MetricCard"
    - path: "frontend-staff/src/pages/ReservationsPage.tsx"
      provides: "All reservations with search/filter/pagination"
      contains: "ReservationCard"
    - path: "frontend-staff/src/components/reservations/ReservationCard.tsx"
      provides: "Individual reservation card with status badge and quick actions"
      contains: "StatusBadge"
  key_links:
    - from: "frontend-staff/src/pages/OverviewPage.tsx"
      to: "/api/v1/staff/overview"
      via: "useTodayOverview hook"
      pattern: "useTodayOverview"
    - from: "frontend-staff/src/pages/ReservationsPage.tsx"
      to: "/api/v1/bookings/staff/"
      via: "useStaffBookings hook"
      pattern: "useStaffBookings"
---

<objective>
Build the Overview dashboard and Reservations list pages -- the two primary operational views staff interact with daily.

Purpose: Overview gives staff immediate situational awareness (today's check-ins, check-outs, occupancy). Reservations list enables searching and filtering all bookings with card-based UI and contextual quick actions.

Output: Fully functional OverviewPage and ReservationsPage replacing their placeholder implementations.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-staff-dashboard/06-CONTEXT.md
@.planning/phases/06-staff-dashboard/06-UI-SPEC.md
@.planning/phases/06-staff-dashboard/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Overview page with metric cards, arrivals list, and departures list</name>
  <files>
    frontend-staff/src/pages/OverviewPage.tsx,
    frontend-staff/src/components/dashboard/MetricCard.tsx,
    frontend-staff/src/components/dashboard/ArrivalsList.tsx,
    frontend-staff/src/components/dashboard/DeparturesList.tsx
  </files>
  <read_first>
    .planning/phases/06-staff-dashboard/06-UI-SPEC.md,
    frontend-staff/src/hooks/queries/useStaffBookings.ts,
    frontend-staff/src/api/types.ts,
    frontend-staff/src/components/common/LoadingSpinner.tsx,
    frontend-staff/src/components/common/EmptyState.tsx,
    frontend-staff/src/components/common/StatusBadge.tsx,
    frontend-staff/src/index.css
  </read_first>
  <action>
**MetricCard (`src/components/dashboard/MetricCard.tsx`):**
- Props: `icon` (LucideIcon), `value` (number | string), `label` (string), `accentColor?` (string)
- Card background: `#1E293B`, border: `1px solid #334155`, rounded-lg (8px)
- Layout: Icon top-left in accent color `#0F766E` (20px), value in 32px semibold (600) `#F1F5F9`, label below in 12px `#94A3B8`
- Add `aria-label` combining label and value: e.g., `aria-label="Today's check-ins: 5"`
- Skeleton loading state: Use shadcn Skeleton component with `animate-pulse`, `#1E293B` on `#0F172A`

**ArrivalsList (`src/components/dashboard/ArrivalsList.tsx`):**
- Props: `arrivals` (BookingResponse[])
- Heading: "Today's Arrivals" (20px semibold)
- Compact card rows: guest name (`guest_first_name guest_last_name`), room type (show room_type_name if enriched, else "Room"), confirmation # in monospace `#94A3B8` (slate-400)
- Right side: "Check In" button (accent `#0F766E`, white text)
- Check In button onClick: For now, just a placeholder that will be wired in Plan 04 (toast "Check-in dialog coming soon" or navigate to /check-in-out). Do NOT implement the dialog here -- Plan 04 handles check-in/out dialogs.
- Max 5 items shown, "View All" link at bottom navigating to /check-in-out
- Empty state: heading "No arrivals today", body "All quiet on the front desk. No guests expected today."
- Card row: `#1E293B` background, `#334155` border-bottom, 16px padding, hover `#283548`

**DeparturesList (`src/components/dashboard/DeparturesList.tsx`):**
- Same structure as ArrivalsList but for departures
- "Today's Departures" heading
- "Check Out" button (accent) -- placeholder onClick for now
- Show room number if room_id is present
- Empty state: heading "No departures today", body "No guests checking out today."

**OverviewPage (`src/pages/OverviewPage.tsx`):**
- Use `useTodayOverview()` hook to fetch data
- Top row: 4 MetricCards in responsive grid
  - Desktop: `grid-cols-4`, Tablet: `grid-cols-2`, Mobile: `grid-cols-1`
  - Cards:
    1. "Today's Check-ins" -- CalendarCheck icon, value = arrivals_count
    2. "Today's Check-outs" -- LogOut icon, value = departures_count
    3. "Occupancy" -- BedDouble icon, value = `${occupancy_percent}%`
    4. "Rooms to Clean" -- Sparkles icon, value = rooms_to_clean
- Below metrics: Two-column layout on desktop (`grid-cols-2`), stacked on mobile
  - Left: ArrivalsList with arrivals data
  - Right: DeparturesList with departures data
- Gap between sections: 32px (xl spacing)
- Loading state: 4 skeleton MetricCards + skeleton list rows
- Error state: Toast "Something went wrong. Please try again."

Update `src/App.tsx` to replace the Overview placeholder with the real OverviewPage component (lazy-loaded or direct import).
  </action>
  <verify>
    <automated>cd frontend-staff && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend-staff/src/pages/OverviewPage.tsx` renders 4 MetricCards using `useTodayOverview()` hook
    - `frontend-staff/src/components/dashboard/MetricCard.tsx` has `aria-label` combining label and value
    - MetricCard value uses 32px semibold (`text-[32px] font-semibold` or equivalent)
    - ArrivalsList shows max 5 items with "View All" link to /check-in-out
    - DeparturesList has empty state with exact copy "No guests checking out today."
    - Overview grid is responsive: 4 cols desktop, 2 tablet, 1 mobile
    - Build succeeds
  </acceptance_criteria>
  <done>
    Overview page shows 4 metric cards with today's operational data, arrivals list with Check In buttons, departures list with Check Out buttons, responsive grid layout, loading skeletons, and empty states.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reservations page with card grid, search/filter, and pagination</name>
  <files>
    frontend-staff/src/pages/ReservationsPage.tsx,
    frontend-staff/src/components/reservations/ReservationCard.tsx,
    frontend-staff/src/components/reservations/SearchFilters.tsx
  </files>
  <read_first>
    .planning/phases/06-staff-dashboard/06-UI-SPEC.md,
    frontend-staff/src/hooks/queries/useStaffBookings.ts,
    frontend-staff/src/api/types.ts,
    frontend-staff/src/components/common/StatusBadge.tsx,
    frontend-staff/src/components/common/EmptyState.tsx
  </read_first>
  <action>
**SearchFilters (`src/components/reservations/SearchFilters.tsx`):**
- Full-width search input at top: placeholder "Search by guest name or confirmation number..."
- Input styling: `#1E293B` background, `#334155` border, `#F1F5F9` text, placeholder `#64748B`, focus ring `rgba(15,118,110,0.3)`
- Below search: filter row with:
  - Status dropdown (shadcn Select): All, Pending, Confirmed, Checked In, Checked Out, Cancelled, No Show
  - Date range: "From" date input and "To" date input (simple date inputs, not fancy date picker -- keep it pragmatic)
- Props: `onSearch(params)` callback with search, status, dateFrom, dateTo
- Debounce search input by 300ms before triggering onSearch
- Use URL search params to persist filter state (same pattern as guest frontend)
- "Search" button in accent `#0F766E` if you want explicit submit, OR auto-search on change

**ReservationCard (`src/components/reservations/ReservationCard.tsx`):**
- Props: `booking` (BookingResponse), `onCheckIn?`, `onCheckOut?`, `onCancel?`, `onView?`
- Card: `#1E293B` background, `#334155` border, rounded-lg (8px), 16px padding
- Hover: `#283548` background, transition 150ms
- Layout:
  - Top row: Guest name (`guest_first_name guest_last_name`) in 14px semibold `#F1F5F9` (left), StatusBadge (right)
  - Middle: Confirmation # in monospace font `#94A3B8`, room type name, check-in/out dates formatted as "Mar 21 - Mar 24" using date-fns `format(date, 'MMM d')`
  - Bottom: Quick action buttons (contextual based on status):
    - status === "confirmed": "Check In" button (accent bg `#0F766E`, white text) + "Cancel Booking" button (destructive outline: transparent bg, `#DC2626` text, `#DC2626` border, hover `rgba(220,38,38,0.15)`)
    - status === "checked_in": "Check Out" button (accent) + "View" button (outline: transparent bg, `#334155` border, `#CBD5E1` text, hover `#283548`)
    - all other statuses: "View" button (outline) only
- All buttons: rounded-md (6px), 14px text, min-height 44px for touch targets

**ReservationsPage (`src/pages/ReservationsPage.tsx`):**
- Uses `useStaffBookings(params)` hook
- State: search, status, dateFrom, dateTo, page (starting at 1)
- Pass params to hook: `{ search, status, date_from: dateFrom, date_to: dateTo, skip: (page-1)*12, limit: 12 }`
- Top: SearchFilters component
- Below: Card grid with responsive columns:
  - Desktop (>= 1280px): `grid-cols-3`
  - Tablet (768-1279px): `grid-cols-2`
  - Mobile (< 768px): `grid-cols-1`
  - Gap: 16px (md spacing)
- Map bookings to ReservationCard components
- Bottom: Numbered pagination using shadcn Pagination component
  - 12 items per page (from CONTEXT.md locked decision)
  - Active page: accent background `#0F766E`, white text
  - Inactive: `#1E293B` background, `#CBD5E1` text
  - Chevron arrows for prev/next
- Loading state: 6 skeleton cards in grid (animate-pulse `#1E293B` on `#0F172A`)
- Empty state: heading "No reservations found", body "Try adjusting your search or filters."
- Check In / Check Out / Cancel button clicks: For now, these are placeholder handlers that will be fully implemented in Plan 04 (check-in/out dialogs). Log to console or show toast "Feature coming soon." The buttons MUST exist with correct styling and conditional visibility.

Update `src/App.tsx` to replace the Reservations placeholder with real ReservationsPage.
  </action>
  <verify>
    <automated>cd frontend-staff && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend-staff/src/pages/ReservationsPage.tsx` uses `useStaffBookings` hook with search/status/date params
    - `frontend-staff/src/components/reservations/ReservationCard.tsx` conditionally renders "Check In" for confirmed, "Check Out" for checked_in
    - ReservationCard has StatusBadge import and usage
    - SearchFilters has placeholder text "Search by guest name or confirmation number..."
    - Pagination uses limit=12 per page
    - Card grid is responsive: `grid-cols-3` on desktop, `grid-cols-2` on tablet, `grid-cols-1` on mobile
    - Empty state text is "No reservations found"
    - Build succeeds
  </acceptance_criteria>
  <done>
    Reservations page shows searchable, filterable card grid with status-based quick action buttons, 12-per-page numbered pagination, responsive 3/2/1 column layout, loading skeletons, and empty state.
  </done>
</task>

</tasks>

<verification>
- `cd frontend-staff && npm run build` succeeds
- OverviewPage renders 4 metric cards + arrivals/departures lists
- ReservationsPage renders card grid with search, filters, pagination
- StatusBadge used on ReservationCard
- Responsive layouts: 4/2/1 for metrics, 3/2/1 for reservation cards
</verification>

<success_criteria>
- Staff can see today's operational snapshot (arrivals, departures, occupancy, cleaning)
- Staff can browse all reservations with card-based UI
- Staff can search by guest name or confirmation number
- Staff can filter by status and date range
- Pagination works at 12 cards per page
- Quick action buttons show/hide based on booking status
</success_criteria>

<output>
After completion, create `.planning/phases/06-staff-dashboard/06-03-SUMMARY.md`
</output>
