# Root project config -- minimal.
# Pytest configuration has moved to per-service pyproject.toml files.
# Run tests per-service: cd services/{svc} && uv run pytest tests/ -x

[project]
name = "hotelbook"
version = "0.1.0"
requires-python = ">=3.12"
